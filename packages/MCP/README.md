# MCP MCP Server

创建 MCP 服务器模板

## Usage

```json
{
  "mcpServers": {
    "MCP": {
      "command": "node",
      "args": ["packages/MCP/bin/server.js"]
    }
  }
}
```

## Development

```bash
cd packages/MCP
npm install
npm test
```
